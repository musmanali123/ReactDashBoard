import React from "react";

const NoAuthPage: React.FC = () => {
  return (
    <div
      style={{
        display: "flex",
        height: "100%",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      No permission yet......
    </div>
  );
};

export default NoAuthPage;
